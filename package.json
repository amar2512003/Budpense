{
  "name": "budpense",
  "private": true,
  "version": "1.0.0",
  "description": "Runs the API and the web app together for development.",
  "scripts": {
    "setup": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently --kill-others-on-fail -n db,api,web -c green,blue,magenta \"npm --prefix backend run db\" \"npm --prefix backend run dev:local\" \"npm --prefix frontend run dev\"",
    "test": "npm --prefix frontend test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
